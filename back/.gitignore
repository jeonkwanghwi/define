# back/ — NestJS (Node/TypeScript) 산출물.
# 이 규칙들은 back/ 서브트리에만 적용된다.

# 의존성
node_modules/

# 빌드 산출물
dist/
build/
*.tsbuildinfo

# SQLite 파일 DB (Prisma)
*.db
*.db-journal
*.sqlite
*.sqlite3
prisma/dev.db*

# 환경변수 / 시크릿
.env
.env.*
!.env.example

# 로그 / 캐시
*.log
npm-debug.*
yarn-debug.*
yarn-error.*
coverage/
.cache/

/**
 * 정의 입력 규칙 — 성의 있는 기록을 유도하기 위한 최소 글자 수.
 * 새 정의·재정의·수정 모든 입력 지점이 이 상수 하나를 공유한다 (기준: trim 후 길이).
 * 백엔드에는 일부러 강제하지 않음 — 이미 저장된 짧은 정의들의 동기화(journal.import)가
 * 깨지지 않도록, 신규 입력만 UI에서 막는다.
 */
export const MIN_DEFINITION_LENGTH = 20;
